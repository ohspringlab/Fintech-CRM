{
  "name": "fintech-crm",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "@vercel/node": "^3.2.29"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
